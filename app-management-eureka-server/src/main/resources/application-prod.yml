server:
  port: 80
eureka:
  client:
    service-url:
      defaultZone: http://eureka.yulece.com/eureka
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: true
  instance:
    prefer-ip-address: true